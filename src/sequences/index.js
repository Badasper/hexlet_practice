import { firstNElements, zip, sameParity, isSafeQueens, union, flatten, sort } from './mainTasks';
import { l, head, tail, filter, map, reduce, isEmpty, cons, toString } from './list';

export const sequencesTask = {
  firstNElements, zip, sameParity, isSafeQueens, union, flatten, sort,
};
export const listTask = {
  l, head, tail, filter, map, reduce, isEmpty, cons, toString,
};
